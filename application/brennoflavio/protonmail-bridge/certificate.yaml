apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mail-certificate
  namespace: protonmail-bridge
spec:
  secretName: mail-tls
  issuerRef:
    name: lets-encrypt
    kind: ClusterIssuer
  commonName: proton.brennoflavio.com.br
  dnsNames:
  - proton.brennoflavio.com.br
