apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: home-vad
  namespace: home-vad
spec:
  imageRepositoryRef:
    name: home-vad
  filterTags:
    pattern: '^(?P<ts>[0-9]{14})$'
    extract: '$ts'
  policy:
    numerical:
      order: asc
