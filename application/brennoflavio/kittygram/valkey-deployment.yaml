apiVersion: apps/v1
kind: Deployment
metadata:
  name: kittygram-valkey
  namespace: kittygram
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kittygram-valkey
  template:
    metadata:
      labels:
        app: kittygram-valkey
    spec:
      containers:
        - name: valkey
          image: valkey/valkey:8.1.8
          args:
            - --maxmemory
            - 96mb
            - --maxmemory-policy
            - allkeys-lru
          ports:
            - containerPort: 6379
          readinessProbe:
            exec:
              command:
                - valkey-cli
                - ping
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            limits:
              memory: 16Mi
            requests:
              cpu: 10m
              memory: 16Mi
