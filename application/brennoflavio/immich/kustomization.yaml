apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: immich
resources:
  - namespace.yaml
  - secret.yaml
  - postgres-pvc.yaml
  - postgres-deployment.yaml
  - postgres-service.yaml
  - redis-pvc.yaml
  - redis-deployment.yaml
  - redis-service.yaml
  - pvc.yaml
  - repository.yaml
  - release.yaml
  - storage-template-secret.yaml
  - storage-template-cron.yaml
configMapGenerator:
  - name: immich-public-values
    files:
      - values.yaml=values.yaml
secretGenerator:
  - name: immich-values
    files:
      - values.yaml=values-encrypted.yaml
configurations:
  - kustomizeconfig.yaml
