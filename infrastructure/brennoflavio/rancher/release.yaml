apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: rancher
  namespace: cattle-system
spec:
  releaseName: rancher
  chart:
    spec:
      chart: rancher
      version: 2.14.3
      sourceRef:
        kind: HelmRepository
        name: rancher
        namespace: cattle-system
  interval: 30m
  install:
    remediation:
      retries: 3
  values:
    hostname: "kubernetes.home.brennoflavio.com.br"
    bootstrapPassword: "admin-password"
    ingress:
      ingressClassName: internal-nginx
      tls:
        source: secret
      extraAnnotations:
        cert-manager.io/cluster-issuer: lets-encrypt-dns01
    privateCA: false
    replicas: 1
